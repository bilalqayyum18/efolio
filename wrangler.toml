name = "bilal-efolio"
compatibility_date = "2024-01-01"
pages_build_output_dir = "dist"

# Cloudflare Pages (Git) — recommended build settings:
#   Build command:          npm run build
#   Build output directory: dist
#   Deploy command:         (leave EMPTY — do not run wrangler here)
#
# Cloudflare uploads `dist/` automatically after a successful build.
# Only use `npm run deploy` for manual deploys from your machine.
